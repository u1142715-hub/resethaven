---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import LandingLayout from "../../../layouts/LandingLayout.astro";

const base = import.meta.env.BASE_URL || "/";
const HUB_URL = `${base}overload-reset/`;

const TITLE = "Do NOT Organise Your Tasks If You’re Overwhelmed (Do This First)";
const DESCRIPTION =
  "Why organising tasks can worsen overload — and the stabilisation-first steps that work when you can’t start.";
---

<BaseLayout title={TITLE} description={DESCRIPTION}>
  <LandingLayout>
    <main class="content">
      <header class="section">
        <p class="kicker">
          <a href={HUB_URL}>← Overload Reset hub</a>
        </p>
        <h1>{TITLE}</h1>
        <p>
          If task lists and planning systems make things worse, stop organising. Use the steps below to stabilise first.
        </p>
      </header>

      <section class="section">
        <h2>Do this first (60 seconds)</h2>
        <ol>
          <li><strong>Close the planner:</strong> shut your task app, notebook, or list.</li>
          <li><strong>Permission to delay:</strong> choose one task you are allowed to delay 24 hours.</li>
          <li><strong>One physical action:</strong> do a task that involves movement and finishes cleanly (2 minutes).</li>
        </ol>
      </section>

      <section class="section">
        <h2>Stabilisation before organisation (5 steps)</h2>
        <ol>
          <li><strong>Name the trap:</strong> “More organising will increase load right now.”</li>
          <li><strong>Reduce inputs:</strong> remove one source of new tasks (email, messages, notifications).</li>
          <li><strong>Choose one stabiliser:</strong> home / admin / money / health. Only one.</li>
          <li><strong>Micro-complete:</strong> finish one small, clearly “done” action.</li>
          <li><strong>Stop early:</strong> end after the micro-completion. No planning.</li>
        </ol>
      </section>

      <section class="section">
        <h2>Bad-day version (minimum viable)</h2>
        <p>
          If you’re flooded: do Step 1 (close the planner) and Step 4 (one small completion). Then stop.
        </p>
      </section>

      <section class="section">
        <h2>Default exit</h2>
        <p>
          If you still feel pulled to organise, return to the hub and choose “Decision Fatigue” or “One-Page Reset.”
        </p>
        <p class="note"><strong>Save this page.</strong> Use it when lists make things worse.</p>
      </section>

      <footer class="section">
        <p><a href={HUB_URL}>Not sure which reset you need? Go back to the Overload Reset hub.</a></p>
      </footer>
    </main>
  </LandingLayout>
</BaseLayout>

<style>
  .content { max-width: 900px; margin: 0 auto; }
  .section { margin: 2rem 0; }
  .kicker { margin: 0 0 1rem; }
  .note { margin-top: 1rem; }
</style>
